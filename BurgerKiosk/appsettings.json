{
    "ConnectionStrings": {
        // 연결문자열 모음
        // 여러 개 넣을 수 있음 (개발용, 운영용 등)

        "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=BurgerKioskDb;Trusted_Connection=True;"
        //       ↑                        ↑                           ↑                       ↑
        // 연결문자열 이름          어느 DB 서버에             어느 DB 에             Windows 인증 사용
        // 코드에서 이 이름으로     접속할지                  접속할지               아이디/비밀번호
        // 찾아서 씀               (Visual Studio 에          (없으면 자동 생성)     없이 접속
        //                        기본 포함된 로컬 DB)
    }
}
//코드 안에 직접 쓰면 → 바꿀 때마다 코드 수정 + 재배포 필요
//appsettings.json 에 쓰면 → 파일만 수정하면 됨
