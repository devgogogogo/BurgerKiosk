<Window x:Class="BurgerKiosk.Views.CartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BurgerKiosk.Views"
        mc:Ignorable="d"
        Title="CartView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!-- 상단 타이틀 -->
        <TextBlock Grid.Row="0"
                   Text="장바구니"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        
        <!-- 장바구니 목록 -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding CartItems}"
                  Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="메뉴"  Width="150" DisplayMemberBinding="{Binding Menu.Name}"/>
                    <GridViewColumn Header="가격"  Width="80" DisplayMemberBinding="{Binding Menu.Price}"/>
                    <GridViewColumn Header="수량" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="-"
                                            Width="25"
                                            Height="25"
                                            Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"/>
                                    <TextBlock Text="{Binding Quantity}"
                                               Width="30"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <Button Content="+"
                                            Width="25"
                                            Height="25"
                                            Command="{Binding DataContext.IncreaseQuantityCommand,RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="합계"  Width="80" DisplayMemberBinding="{Binding TotalPrice}"/>
                    <GridViewColumn Header="삭제" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="삭제"
                                        Width="60"
                                        Height="25"
                                        Command="{Binding DataContext.RemoveItemCommand,RelativeSource={RelativeSource AncestorType=ListView} }"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <!-- Horizontal — 가로로 나열 -->
        <!-- Vertical — 세로로 나열 (기본값) -->
        <!-- 총 금액 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="10">
            <TextBlock Text="총금액 : "
                      FontSize="18"
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalPrice}"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <TextBlock Text="원"
                       FontSize="18"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 버튼 -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="10">
            <Button Content ="뒤로가기"
                    Width="100"
                    Height="40"
                    Margin="5"
                    Click="BackButton_Click"/>
            <Button Content ="주문완료"
                    Width="100"
                    Height="40"
                    Margin="5"
                    Command="{Binding CompleteOrderCommand}"/>
        </StackPanel>

    </Grid>
</Window>
